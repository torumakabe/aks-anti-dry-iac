linters:
  disable-all: true
  enable:
    - deadcode
    - errcheck
    - errorlint
    - wrapcheck
    - gosimple
    - govet
    - ineffassign
    - staticcheck
    - structcheck
    - typecheck
    - varcheck
    - bodyclose

linters-settings:
  govet:
    check-shadowing: false

issues:
  exclude-rules:
    - path: version\.go
      linters:
        - deadcode
